{
  "name": "copy-line",
  "displayName": "Copy Line",
  "publisher": "qinhaoyu",
  "version": "0.7.0",
  "description": "Copy file path and line numbers to clipboard. Perfect for collaborating with AI assistants to locate code.",
  "engines": {
    "vscode": "^1.104.0"
  },
  "categories": [
    "Other"
  ],
  "keywords": [
    "copy",
    "file",
    "line",
    "ai",
    "assistant",
    "reference",
    "path",
    "line numbers",
    "file name",
    "AI assistant"
  ],
  "license": "MIT",
  "main": "./out/extension.js",
  "repository": {
    "type": "git",
    "url": "https://github.com/whale2002/copy-line.git"
  },
  "bugs": {
    "url": "https://github.com/whale2002/copy-line/issues"
  },
  "homepage": "https://github.com/whale2002/copy-line#readme",
  "icon": "icon.png",
  "activationEvents": [
    "onCommand:copy-line.copySelectionReference"
  ],
  "contributes": {
    "commands": [
      {
        "command": "copy-line.copySelectionReference",
        "title": "Copy Selected File and Line Numbers",
        "icon": "$(go-to-file)"
      }
    ],
    "menus": {
      "editor/context": [
        {
          "command": "copy-line.copySelectionReference",
          "when": "editorTextFocus",
          "group": "copy-line@1"
        }
      ],
      "editor/title": [
        {
          "command": "copy-line.copySelectionReference",
          "when": "editorTextFocus",
          "group": "copy-line@1"
        }
      ]
    },
    "keybindings": [
      {
        "command": "copy-line.copySelectionReference",
        "key": "cmd+alt+l",
        "mac": "cmd+alt+l",
        "when": "editorTextFocus"
      }
    ],
    "configuration": {
      "type": "object",
      "title": "Copy Line",
      "properties": {
        "copy-line.outputFormat": {
          "type": "string",
          "enum": [
            "labeled",
            "compact",
            "code-style",
            "natural"
          ],
          "default": "labeled",
          "description": "Output format: labeled (e.g., File: src/file.ts (line 24-30)), compact (e.g., src/file.ts:24-30), code-style (e.g., src/file.ts:24:30), natural (e.g., line 24 in src/file.ts)"
        },
        "copy-line.useAbsolutePath": {
          "type": "boolean",
          "default": false,
          "description": "Use absolute paths instead of relative paths"
        },
        "copy-line.showStatusMessage": {
          "type": "boolean",
          "default": true,
          "description": "Show status bar message after copying"
        },
        "copy-line.singleLineFormat": {
          "type": "string",
          "default": "line ${line}",
          "description": "Format for single line selection"
        },
        "copy-line.multiLineFormat": {
          "type": "string",
          "default": "line ${start}-${end}",
          "description": "Format for multi-line selection"
        }
      }
    }
  },
  "scripts": {
    "compile": "tsc -p .",
    "watch": "tsc -w -p .",
    "vscode:prepublish": "npm run compile",
    "build": "vsce package -o ./release",
    "deploy": "vsce publish"
  },
  "devDependencies": {
    "@types/node": "^20.19.25",
    "@types/vscode": "^1.104.0",
    "typescript": "^5.9.3"
  }
}
